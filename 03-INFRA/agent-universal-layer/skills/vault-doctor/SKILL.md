---
name: vault-doctor
description: Deprecated alias for nexgen-doctor. Use when the user typed /vault-doctor out of habit; prefer nexgen-doctor.
---

# Renamed to `nexgen-doctor`

This command acts on the engine and its tooling, not on the Vault. The
`vault-` prefix said the opposite, so the skill is now `nexgen-doctor`. Memory
commands keep the `vault-` name, because the memory is the Vault.

Do not follow a runbook from here, there is deliberately only one copy so the
two names cannot drift apart:

1. Load the `nexgen-doctor` skill and follow it exactly.
2. Mention once, in passing, that the command is now `nexgen-doctor`. Do not make a
   lesson of it and do not stop to ask.
