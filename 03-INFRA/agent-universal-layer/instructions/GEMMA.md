# Gemma Local Model Alias

Legacy compatibility pointer for older `gemma-worker`/`gemma-agent` references
(kept so any script or shortcut still targeting them keeps resolving). The
full local-worker policy lives in `LOCAL-WORKER.md` — read and follow that
file, not this one; this file has no policy content of its own.

Canonical policy order:

1. `AGENTS.md`
2. `LOCAL-WORKER.md`
